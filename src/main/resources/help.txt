Burp Extension APIs do not allow access to the Intruder results table.
This implementation relies on hooks in the send/receive pipeline, which have additional delay due to Burp CPU time.
However, this should not significantly impact statistical evaluation, as adding a somewhat fixed delay (which averages out over many requests) should not change the bottom line.
What matters is the relative difference between times.