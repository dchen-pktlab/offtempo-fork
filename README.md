# Miao

Build with:

```sh
./gradlew bigJar
```

Then in Burp, go to Extensions tab and load the jar from `build/libs/`.

Hit ctrl+click on the extension to reload after you rebuild (this avoids a dialog).
